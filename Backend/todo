CSV Upload :	Users upload CSV files containing image URLs and metadata.
Schema Detection:	System detects CSV headers and attempts to auto-match with your internal schema.
User Column Mapping:	If auto-matching is incomplete or inaccurate, users map their CSV columns to your schema.
Data Normalization:	CSV data is transformed to your internal format using the mappings.
Validation & Error Handling:	Validate transformed data, report errors, and request corrections if needed.
Image Download & Processing:	Download images from URLs and preprocess them for model training.
Live Model Training	Train/update: ML model based on the normalized dataset.
API & Frontend	REST API and frontend UI for uploads, mappings, validation, and training status.
